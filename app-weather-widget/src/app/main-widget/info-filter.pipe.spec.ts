import { InfoFilterPipe } from './info-filter.pipe';

describe('InfoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new InfoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
